# chatbot-dialog-creator

This tool can be used to generate dialog related data. That data can be used to 
feed a torch based chatbot.

## Preparations
Linux:
```bash
$ pip3 install PyYAML
```

Windows:
```ps1
pip install PyYAML
```
